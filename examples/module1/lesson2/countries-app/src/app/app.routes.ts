import { Routes } from '@angular/router';
import { AppContainerComponent } from './app-containter/app-container.component';

export const routes: Routes = [
  {path: '', component: AppContainerComponent}
];
